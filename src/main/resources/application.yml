spring:
  application:
    name: smart-backend

  # 1. 데이터베이스 설정 (기존 내용 유지)
  datasource:
    url: jdbc:mysql://database-1.cnwayga6k6j6.us-east-1.rds.amazonaws.com:3306/smart_sourcing_db?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: admin
    password: wjddnjswns
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      connection-test-query: SELECT 1
      validation-timeout: 5000
      max-lifetime: 1800000

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true

  # 2. AWS SQS 설정 (기존 내용 유지)
  cloud:
    aws:
      region:
        static: us-east-1
      sqs:
        # SQS 대기열 주소
        endpoint: https://sqs.us-east-1.amazonaws.com/730335221432/smart-sourcing-queue

  # ⭐️ 3. Amazon ElastiCache 설정 (최종 반영)
  data:
    redis:
      # 👇 EC2 IP (10.0.1.245) 대신 ElastiCache Serverless 엔드포인트 반영
      host: smart-sourcing-cache-4woaoh.serverless.use1.cache.amazonaws.com
      port: 6379
      # Redis 접속에 필요한 기타 설정 (필요 시 추가)
      # password:
      # timeout: 5000ms

server:
  port: 8080