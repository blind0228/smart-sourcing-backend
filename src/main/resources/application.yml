spring:
  application:
    name: smart-backend

  # 1. 데이터베이스 설정 (기존 내용 유지)
  datasource:
    url: jdbc:mysql://database-1.cnwayga6k6j6.us-east-1.rds.amazonaws.com:3306/smart_sourcing_db?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: admin
    password: wjddnjswns
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true

  # 2. AWS SQS 설정 (새로 추가된 부분)
  cloud:
    aws:
      region:
        static: us-east-1
      # 👇 유효한 AWS 자격 증명을 직접 추가하는 섹션
      sqs:
        # SQS 대기열 주소
        endpoint: https://sqs.us-east-1.amazonaws.com/730335221432/smart-sourcing-queue

server:
  port: 8080